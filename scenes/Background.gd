extends ParallaxBackground

# func _process(_delta):
# 	if Globals.current_camera:
# 		var size = Globals.current_camera.get_viewport_rect().size
# 		%Sprite2D.region_rect = Rect2(Vector2(0, 0), size)
